import { Container } from "../../Container"
import { Header, } from "../../Header"
export const WeatcherApp = () => {
    return (
        <Container>
            <Header />
        </Container>
    )
}